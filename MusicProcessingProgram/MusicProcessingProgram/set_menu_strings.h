#ifndef SET_MENU_STRINGS_H
#define SET_MENU_STRINGS_H

#include <vector>
#include <string>

std::vector<std::string> set_main_menu();

std::vector<std::string> set_save_menu();

std::vector<std::string> set_load_menu();
#endif 